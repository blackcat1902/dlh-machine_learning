# dlh-machine_learning/pipeline/pandas
# From NumPy to DataFrame
This project provides a function `from_numpy(array)` that converts a NumPy ndarray into a pandas DataFrame. The columns are labeled alphabetically (A, B, C, ...) and capitalized. The project follows all required Python and documentation standards.
